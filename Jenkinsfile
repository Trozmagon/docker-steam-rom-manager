/* ##############################################################################
 * This Jenkinsfile is a stub.
 *
 * In the upstream linuxserver/docker-* repositories this file is auto-generated
 * by the LinuxServer.io Jenkins Builder
 * (https://github.com/linuxserver/docker-jenkins-builder) from `jenkins-vars.yml`.
 *
 * For a local / standalone build you don't need this file at all — just run:
 *
 *   docker build --no-cache --pull -t lscr.io/linuxserver/steam-rom-manager:latest .
 *
 * If you fork this repo into the linuxserver org and want the real pipeline,
 * re-generate this file with the LSIO Jenkins Builder.
 * ############################################################################## */

pipeline {
    agent any
    stages {
        stage('Notice') {
            steps {
                echo 'This Jenkinsfile is a stub. Use the LSIO Jenkins Builder to regenerate it from jenkins-vars.yml.'
            }
        }
    }
}
